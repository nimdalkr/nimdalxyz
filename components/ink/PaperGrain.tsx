/**
 * Paper fibre. One fixed, pointer-transparent layer of procedural grain so the
 * ground reads as cold-press paper rather than a flat hex value.
 */
export function PaperGrain() {
  return <div className="riso-grain" aria-hidden />;
}
